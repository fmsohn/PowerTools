/** localStorage key for sticky broker split (0–1). */
export const ENERGY_BROKER_SPLIT_STORAGE_KEY = 'energy_broker_split'

/** Default when no persisted broker split (full split to broker). */
export const ENERGY_BROKER_SPLIT_DEFAULT = 1.0

/** Margin above this triggers high-margin UX warning ($/kWh decimal). */
export const MARGIN_WARNING = 0.03

/** Default contract term for estimates (months). */
export const TERM_DEFAULT = 12
